source 'https://rubygems.org'

gemspec

# Regenerated for TDLib 1.8.64; not published to rubygems (see gemspec constraint).
# Local dev: bundle config set --local local.tdlib-schema ~/Desktop/tdlib-schema
gem 'tdlib-schema', github: 'anko20094/tdlib-schema', branch: 'master'

group :development do
  gem 'kramdown'
end
